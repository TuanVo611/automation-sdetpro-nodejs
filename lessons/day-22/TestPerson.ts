import Person from "./Person";

let teo = new Person("Teo", 18);

/**
 * Control the access of class's properties of methods: Control validity of the object's data
 * - What can be accessed ?
 * - Where can be acceessed ?
 */

